# Production site URL (canonical, sitemap, OG). No trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX). Leave empty to disable.
NEXT_PUBLIC_GA_ID=

# Optional: ICE news API cache TTL in seconds (default 60).
# ICE_NEWS_CACHE_TTL=60

# Optional: AI summarization. OFF by default so you can go live without using your own API.
# To enable: set NEXT_PUBLIC_AI_ENABLED=true AND set one of the keys below.
# Leave NEXT_PUBLIC_AI_ENABLED unset or false = no AI section shown, no keys needed.
# NEXT_PUBLIC_AI_ENABLED=true

# Server-only (never sent to browser). Only used when AI is enabled above.
# OpenAI: https://platform.openai.com/api-keys  (uses gpt-4o-mini)
# Google: https://aistudio.google.com/apikey     (uses Gemini 1.5 Flash)
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...

# When AI is enabled and strangers use the app: limit requests per IP per hour (default 10).
# AI_RATE_LIMIT_PER_HOUR=10
